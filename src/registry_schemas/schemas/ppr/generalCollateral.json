{
    "definitions": {},
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://bcrs.gov.bc.ca/.well_known/schemas/ppr/generalCollateral",
    "type": "object",
    "title": "The PPR General Collateral Schema",
    "properties": {
        "description": {
            "type": "string",
            "minLength": 5,
            "maxLength": 4000,
            "description": "The description of the general collateral and proceeds being claimed in accordance with the legislative requirements."
        },
        "addedDateTime": {
            "type": "string",
            "format": "date-time",
            "description": "The time when this collateral item was registered in the PPR system."
        },
        "collateralId": {
            "type": "integer",
            "mimimum": 1,
            "description": "The unique identifier generated by the PPR system for the General Collateral record. Returned in the API responses, it is required in requests when removing or modifying General Collateral with an Amendment Statement or a Change Statement."
        }
    },
    "required": [
        "description"
    ]
}