{
    "definitions": {},
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://bcrs.gov.bc.ca/.well_known/schemas/ppr/vehicleCollateral",
    "type": "object",
    "title": "The PPR Vehicle Collateral Schema",
    "properties": {
        "type": {
            "type": "string",
            "enum": [ "AC", "AF", "BO", "EV", "MH", "MV", "OB", "TR"],
            "maxLength": 2,
            "description": "Specifies the type of vehicle."
        },
        "serialNumber": {
            "type": "string",
            "maxLength": 25,
            "description": "Serial number of the vehicle (eg, VIN number, DOT number (if plane registered in Canada), Airframe number if registered outside of Canada, or designated serial number of the collateral."
        },
        "make": {
            "type": "string",
            "maxLength": 25,
            "description": "Make of vehicle (or manufacturer's name)."
        },
        "model": {
            "type": "string",
            "maxLength": 25,
            "description": "Model name."
        },
        "year": {
            "type": "integer",
            "mimimum": 1900,
            "maximum": 2100,
            "description": "Year of manufacture of the collateral (or model year of a automobile)."
        },
        "manufacturedHomeRegistrationNumber": {
            "type": "string",
            "minLength": 1,
            "maxLength": 7,
            "description": "Home registration number assigned by the Manufactured Home Registry."
        },
        "vehicleId": {
            "type": "integer",
            "mimimum": 1,
            "description": "The unique identifier generated by the PPR system for the Vehicle Collateral record. Returned in the API responses, it is required in requests when removing or modifying Vehicle Collateral with an Amendment Statement or a Change Statement."
        }
    },
    "required": [
        "type",
        "serialNumber"
    ]
}